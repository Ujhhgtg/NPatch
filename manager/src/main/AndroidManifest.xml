<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- Matches WeKit: blur is runtime-gated; API 28-32 retain opaque Material surfaces. -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <!-- APK file management supports legacy file:// imports as well as SAF. Access is
         optional and granted by the user; the Play distribution-policy check is inapplicable. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSTALL_PACKAGE" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
    </queries>

    <application
        android:name=".LSPApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_playstore"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        
        <!-- 专门为小米手机桌面卸载添加了提示，提升用户体验 -->
        <meta-data
            android:name="app_description_title"
            android:resource="@string/miui_uninstall_title" />
        <meta-data
            android:name="app_description_content"
            android:resource="@string/miui_uninstall_content" />

        <activity
            android:name=".ui.activity.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity-alias
            android:name=".ui.activity.InstallActivity"
            android:enabled="false"
            android:exported="true"
            android:targetActivity=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="application/vnd.android.package-archive" />
            </intent-filter>
        </activity-alias>

        <receiver
            android:name=".install.InstallResultReceiver"
            tools:ignore="ExportedReceiver"
            android:exported="true" />


        <service
            android:name=".manager.ModuleService"
            android:exported="true"
            tools:ignore="ExportedService" />


        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Cross-application bridges are part of the existing module protocol.
             Keep their established exported/permission behavior during Lint-only cleanup. -->
        <provider
            android:name=".manager.ConfigProvider"
            android:authorities="top.nkbe.npatch.manager.provider.config"
            tools:ignore="ExportedContentProvider"
            android:exported="true"
            android:enabled="true" />

        <provider
            android:name=".manager.RemoteApiProvider"
            android:authorities="${applicationId}.remote"
            tools:ignore="ExportedContentProvider"
            android:enabled="true"
            android:exported="true" />

    </application>
</manifest>
